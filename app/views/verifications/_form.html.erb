<%= form_for(verification) do |f| %>
  <% if verification.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(verification.errors.count, "error") %> prohibited this verification from being saved:</h2>

      <ul>
      <% verification.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :type_of_verification %>
    <%= f.text_field :type_of_verification %>
  </div>
  
  

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
